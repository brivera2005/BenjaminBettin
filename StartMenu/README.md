# Deprecated

This Python start menu has been replaced by **Black Glass Start Menu**, a fork of Open-Shell.

Use **`../BlackGlassStart/install.bat`** instead.

Open-Shell is a native Windows shell replacement that actually replaces the Start menu — no keyboard hooks, no tray workarounds.
